<% content_for :title, "Nuevo actor" %>

<div class="card">
  <div class="card-header">
    <h1 class="h3 mb-0">Nuevo actor</h1>
  </div>
  <div class="card-body">
    <%= render "form", actor: @actor %>
  </div>
</div>

<div class="mt-3">
  <%= link_to "Volver a actores", actors_path, class: "btn btn-outline-secondary" %>
</div>
