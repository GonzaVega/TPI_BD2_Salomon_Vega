<% content_for :title, "Nuevo contenido" %>

<div class="card">
  <div class="card-header">
    <h1 class="h3 mb-0">Nuevo contenido</h1>
  </div>
  <div class="card-body">
    <%= render "form", catalogo: @catalogo %>
  </div>
</div>

<div class="mt-3">
  <%= link_to "Volver al catálogo", catalogos_path, class: "btn btn-outline-secondary" %>
</div>
